name: "+ Update aqua users list of README"
on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 0 * * *" # daily

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Push a commit to the main branch
    steps:
      - uses: actions/checkout@c85c95e3d7251135ab7dc9ce3241c5835cc595a9 # v3.5.3
      - uses: actions/setup-go@fac708d6674e30b6ba41289acaab6d4b75aa0753 # v4.0.1
        with:
          go-version: 1.20.6

      - id: generate_token
        uses: tibdex/github-app-token@021a2405c7f990db57f5eae5397423dcc554159c # v1
        with:
          app_id: ${{secrets.APP_ID_AQUAPROJ_AQUA}}
          private_key: ${{secrets.APP_PRIVATE_KEY_AQUAPROJ_AQUA}}

      - uses: aquaproj/aqua-installer@36dc5833b04eb63f06e3bb818aa6b7a6e6db99a9 # v2.1.2
        # Install ghcp
        with:
          aqua_version: v2.9.0
        env:
          AQUA_GITHUB_TOKEN: ${{github.token}}
      - run: ghcp -v
        env:
          GITHUB_TOKEN: ${{github.token}}

      - run: go run ./cmd/list-aqua-users | tee result.md
        env:
          GITHUB_TOKEN: ${{github.token}}
      - run: cat docs/HEADER.md result.md docs/FOOTER.md > README.md
      - id: create-branch
        run: |
          branch=update-list-$(date +%Y%m%d%H%M%S)
          echo "branch=$branch" >> "$GITHUB_OUTPUT"
          ghcp commit -r "$GITHUB_REPOSITORY" -m "docs: update README.md" -b "$branch" README.md
        env:
          GITHUB_TOKEN: ${{steps.generate_token.outputs.token}}
      - run: |
          body="This pull request is created by [GitHub Actions]($GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID)."
          gh pr create -t "docs: update README.md" -H "$BRANCH" -b "$body"
        env:
          BRANCH: ${{steps.create-branch.outputs.branch}}
          GITHUB_TOKEN: ${{steps.generate_token.outputs.token}}
